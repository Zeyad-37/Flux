# Flux
A Flow based Redux (MVI) pattern for a multi module Clean Arch android sample app with Jetpack Compose, Navigate and Hilt with tests

## TODO
- Migrate from reducers to input handlers
- organize modules
- update samples
- publish
- update dependencies
